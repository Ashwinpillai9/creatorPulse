name: Daily Newsletter
on:
  schedule:
    - cron: "30 2 * * *"  # 08:00 IST (UTC+5:30)
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger newsletter send
        run: |
          curl -X POST https://<your-hf-space>.hf.space/newsletter/send || true
